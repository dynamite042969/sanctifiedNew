const path = require('path');
const CopyPlugin = require('copy-webpack-plugin');

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed pdfkit specific webpack configuration
};

module.exports = nextConfig;
